# collects match data from riots offical api, stores in sqlite dbase and builds a basic neural net which predicts the unknown champion in a given draft. 
# run train.py to train the model. the model is stored in artifacts/checkpoints and diagnostic figures in artifacts/figures
